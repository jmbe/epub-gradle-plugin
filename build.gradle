plugins {
  id 'java-gradle-plugin'
}

repositories {
  mavenLocal()
  jcenter()
}

dependencies {
  compile 'org.idpf:epubcheck:4.0.2'
}

gradlePlugin {
  plugins {
    simplePlugin {
      id = 'se.intem.epub-plugin'
      implementationClass = 'se.intem.epub.EpubPlugin'
    }
  }
}
